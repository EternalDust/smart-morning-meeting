server:
  port: 8086

spring:
  application:
    name: smart-morning-meeting-backend
  
  # Redis 缓存集群配置 (此处以单机做示例，部署时改为cluster)
  data:
    redis:
      host: localhost
      port: 6379
      password: ""
      database: 0
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

  # MySQL 数据源配置
  datasource:
    url: jdbc:mysql://localhost:3306/smart_meeting?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

# MyBatis-plus 配置
mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
  global-config:
    db-config:
      id-type: auto

# 大模型复盘报告配置 (Qwen / DeepSeek 等 OpenAI 兼容接口)
# base-url 留空时后端降级为模板模拟生成，保证演示链路可用
ai:
  llm:
    base-url: ${AI_LLM_BASE_URL:}
    model: ${AI_LLM_MODEL:qwen-plus}
    api-key: ${AI_LLM_API_KEY:}
    connect-timeout-seconds: 10
    read-timeout-seconds: 60
    temperature: 0.7
    max-tokens: 1500
    max-retry: 3
