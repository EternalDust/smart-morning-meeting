server:
  port: 8084

spring:
  application:
    name: smart-meeting-supervise

  datasource:
    url: jdbc:mysql://localhost:3306/smart_meeting?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  redis:
    host: localhost
    port: 6379
    database: 0

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.huadi.smm.supervise.entity
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true

jwt:
  secret: smart-morning-meeting-2026
  expiration: 86400000